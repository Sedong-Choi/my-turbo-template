{
  "name": "turbo-storybook-nextui-template",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "storybook": "pnpm --filter storybook storybook",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "web:prisma-studio": "pnpm --filter web db:studio"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.1.0",
  "engines": {
    "node": ">=18"
  }
}
